<div class="card_checkbox">
  <input type="checkbox" name="cards[]" id="<%= "#{card}_checkbox" %>" value="<%= card %>">
  <label for="<%= "#{card}_checkbox" %>">
    <%= render partial: "cards/card_png", locals: { card: card } %>
  </label>
</div>
