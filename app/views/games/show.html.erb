<div id="<%= dom_id(gvm) %>" class="relative_game_container">
  <%= render partial: "game_play_container", locals: { gvm: gvm, app: app } %>
</div>
<%= turbo_stream_from gvm.get_stream_id_for_user(user) %>
